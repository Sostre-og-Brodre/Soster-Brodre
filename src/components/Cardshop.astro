---
const { ShopData } = Astro.props;
import RundKnap from "../components/RundKnap.astro";
---

<article class="card">
     <div class="tekstogknap"><img src={ShopData.Photo} alt={ShopData.Name} />
    <p><strong>{ShopData.Name}</strong></p>
  <p>{ShopData.Price}kr</p>
    <RundKnap tekst="KØB NU" href={"/button/" + ShopData.slug} /></div>
</article>

<style>
  article {
    display: grid; 
    grid-row: span 5;
     grid-template-rows: subgrid;
    align-items: start; 
    gap: 0.75rem;
   }
 .tekstogknap { 
  display: grid; 
    grid-row: span 4;
     grid-template-rows: subgrid;
    align-items: start; 
    gap: 0.75rem;}

  

  img {
    width: 100%;
    aspect-ratio: 0.8;
    object-fit: cover;
    border-radius: 4px;
     border: 10px solid #a3bfb26b; */
   
  }

   p {
    text-align: center;}


</style>